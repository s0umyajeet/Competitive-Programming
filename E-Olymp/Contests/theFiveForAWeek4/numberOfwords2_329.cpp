#include <bits/stdc++.h>
using namespace std;

int main() {
        vector<string> v;
        string input;
        while (cin >> input) {
                v.push_back(input);
        }
        cout << v.size() << endl;
        return 0;
} 